import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <main>
      <i className="ph ph-browser"></i>
    </main>
  )
}

export default App
